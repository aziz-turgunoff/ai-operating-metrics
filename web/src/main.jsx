import React from "react";
import { createRoot } from "react-dom/client";
import AIOperatingMetrics from "./AIOperatingMetrics.jsx";

createRoot(document.getElementById("root")).render(<AIOperatingMetrics />);
